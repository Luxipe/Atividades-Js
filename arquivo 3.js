// Declarando variáveis
let nome = "João";
let idade = 25;

// Criando mensagem usando operadores de concatenação
let mensagem = "Nome: " + nome + ", Idade: " + idade;

// Exibindo a mensagem
console.log(mensagem);
